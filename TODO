- y si el proceso lanzado en terminal necesita interactuar con el usuario?

- mostrar el proceso actual que se ejecuta en otro campo de texto.

-no enviar parámetros cuando es vacío (ejemplo de ping con modificador): el ping no se ejecuta por error en parametro
